# historical_code

Archived code and backups live here so the repository root stays focused on active TianAim development.

Current contents:

- `dev-branch/`: legacy ROS2 and training/reference material after the active ROS2 move to `ros2_ws/`
- `tianboard_s/`: board-level reference project
- `_git_migration_backup/`: migration backup snapshot
- `stm32_gimbal_control.git.BAK-20260319/`: retained nested Git backup from the firmware project

Do not add new runtime work here. Use these files only for comparison, recovery, or migration reference.
