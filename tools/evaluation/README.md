# tools/evaluation

这里放评估工具。当前低速跟随已经顺滑，高速跟随仍未完成，因此评估重点应放在高速场景。

建议评估指标：

- 检测是否连续。
- 目标中心点是否跳变。
- 桥接发送坐标是否滞后。
- 下位机 `error_x/error_y` 峰值和持续时间。
- `yaw_add_mrad/pitch_add_mrad` 是否打到限幅。
- 云台是否过冲、拖尾或跟不上。

